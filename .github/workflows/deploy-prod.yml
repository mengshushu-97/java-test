name: Deploy Prod

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: Type deploy-prod to confirm production deployment
        required: true

concurrency:
  group: java-test-prod
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Build and deploy java-test to prod
    if: ${{ github.event.inputs.confirm == 'deploy-prod' }}
    runs-on:
      - self-hosted
      - k3s
      - prod
    environment: production
    env:
      APP_NAME: java-test
      CONTAINER_NAME: java-test
      NAMESPACE: prod
      MANIFEST_DIR: k8s/prod
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Runner preflight
        env:
          CHECK_NAMESPACE: prod
        run: ./scripts/check-runner-prereqs.sh

      - name: Test
        run: |
          mkdir -p /tmp/m2-java-test
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -v /tmp/m2-java-test:/root/.m2 \
            -w /workspace \
            maven:3.9-eclipse-temurin-17 \
            mvn test

      - name: Build local image and deploy to k3s
        run: ./scripts/deploy-local-k3s.sh
